To install the needed Python libraries simply run:
```pip install -r requirements.txt```

To run the RAG simply type the following command into the terminal:
```python -m streamlit run chat.py```

To upload PDFs to courses and to add/delete courses go to the lecturers page which is found at localhost/lecturers. The current password is set to admin but this can be changed in pages/_lecturers.py